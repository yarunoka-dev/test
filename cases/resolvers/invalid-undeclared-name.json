{
    "description": "A name that is used, not defined under date_sets, and not declared under resolvers is rejected - the language never leaves a name silently to the host",
    "spec": "resolvers - every name that is used and not defined must be declared",
    "request": {
        "action": "eval",
        "document": {
            "version": "1.0",
            "timezone": "Asia/Tokyo",
            "schedules": [
                {"days": ["team-days"], "times": ["09:00"]}
            ]
        },
        "query": {"type": "point", "at": "2026-07-27T09:00:00+09:00"}
    },
    "response": {"invalid": true}
}
